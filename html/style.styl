*{
  background-color: #f1f1f1;
  margin: 0;
  padding: 0;
}

.navbar{
  height: 94px;
  width: 100%;
}

.navbar ul {
    list-style-type: none; /*this make the bullets disapear*/
    margin: 0;
    padding: 15px 125px;
    top: 0;
    box-shadow: 0px 0px 10px 0px rgba(0,0,0,0.25);
    background-color: white;
    /*width: 25%;*/
    /*height: 100%;Full Height*/
    /*position: fixed;  Make it stick, even on scroll (it can be 'sticky' aswell) - the diff is that
    the element will sticky to the page when the page scrolls out of it (it needs to specify where to 
    sticky the element (top, right, bottom or left)*/
    /*overflow: auto;  Enable scrolling if the sidenav has too much content */
    overflow: hidden;
    /*border: 1px solid #555;*/
  }

  li{
    float: left;
    text-align: center;
    background-color: white;
    /*border-right: 1px solid #555;*/
  }

  li:last-child{
    border-right: none;
    float: right;
  }

  .logotype{
    width: 241px;
    height: 90px;
    }

  li a{
    background-color: white; 
    display: block;
    color: #000;
    padding: 14px 16px;
    text-decoration: none;
  }
  li a.active {
    font-weight: bold;
    color: #DC143C;
  }

  li a:hover:not(.active){
    color: #FF0000;
  }

  li.dropdown{
    display: inline-block;
  }

  .dropdown-menu {
    display: none;
    position: absolute;
    min-width: 160px;
    box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
    z-index: 1;
    animation: growDown 300ms ease-in-out forwards;
    transform-origin: top center;
  }

  .dropdown-menu a{
    color: black;
    padding: 12px 16px;
    text-decoration: none;
    display: block;
    text-align: left;
  }

  .dropdown-content
    position: absolute;
    min-width: 160px;
    box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
    z-index: 1;
    for num in (1..5)
      .dropdown-item-{num}
        animation: rotateX 300ms ease-in-out (num * 60ms) forwards
        transform-origin: top center
  

.dropdown-content a{
  color: black;
  display: none;
  opacity: 0;
  padding: 12px 16px;
  opacity: 0;
}

.dropdown-menu a:hover {
  color: #FF0000;
}

.dropdown-content a:hover {
  color: #FF0000;
}

.dropdown:hover .dropdown-menu {
  display: block;
}

.dropdown:hover .dropdown-content a{
  display: block;
}

.fa-caret-down{
  transition: .5s;
  transform: rotate(0deg);
  }

.dropdown:hover .fa-caret-down {
  transform: rotateX(180deg);
  }

@keyframes rotateX {

  0% {
    opacity: 0;
    transform: rotateX(-90deg);
  }

  50% {
    transform: rotateX(-20deg);
  }

  100% {
    opacity: 1;
    transform: rotateX(0deg);
  }

}

@keyframes growDown {

  0% {

      transform: scaleY(0)

  }

  80% {

      transform: scaleY(1.1)

  }

  100% {

      transform: scaleY(1)

  }

}

  /*responsive stuff*/
  @media screen and (max-width: 810px) {
    li:last-child, 
    ul li {float: none;}    
  }